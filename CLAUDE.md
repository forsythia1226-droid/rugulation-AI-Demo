# 사내규정 AI 에이전트 (taihan) — 파일럿 데모

대한전선 사내 AI 에이전트 구축 사례 대회 출품작.
사내규정 50건을 AI 창구 하나로 조회하고, 답변마다 근거 조문을 원문에 하이라이트한다.
**GitHub Pages 공개 주소로 제출 예정.**

## 왜 만드는가

현업이 규정을 찾는 현재 절차:
게시판 검색 → 규정 파일 다운로드 → Ctrl+F → 판단 → 애매하면 주관팀에 전화.
경력직이 대거 채용됐지만 사내 규정 교육이 따로 없다.
전자결재 도입으로 서면보고와 별개로 전자결재를 반드시 올려야 하는데, 위임전결 규정을 뒤져 결재선을 찾아야 한다.

## 설계 원칙 (기능을 더하거나 뺄 때 기준)

1. 규정 답변은 정답보다 근거가 중요하다. 답과 원문을 끝까지 붙여둔다.
2. 현업은 규정을 알고 싶은 게 아니라 일을 끝내고 싶다. 절차·서식·기한까지가 답이다.
3. 경력직은 자기가 뭘 모르는지 모른다. 검색(pull) 외에 알림·체크리스트(push)가 필요하다.

## 대회 평가 기준

아이디어와 현업 페인포인트 해결력. 구현 완성도가 아니다.
→ UI/UX와 기능 구성이 최우선. 백엔드는 설계서로 갈음한다.
대회명이 "AI 에이전트"이므로 "답만 하는 챗봇"이 아니라 일을 대신 하는 흐름이 보여야 한다.

## 파일 구조

```
index.html          화면 골격 (사이드바 / 헤더 / 본문 컨테이너)
css/style.css       디자인 토큰 + 전체 스타일 (다크모드 포함)
js/regulations.js   규정 데이터: CATS(업무분류), D(규정 레지스트리), reg()
js/store.js         저장 계층(localStorage): 시연 로그인·설정·규정 편집 오버레이·확인 요청·질의 로그. 운영 시 백엔드 API로 대체
js/pages.js         로그인·설정·입사자 체크리스트·내 확인 요청·규정 인쇄/다운로드
js/admin.js         관리자: 규정 등록/수정/삭제, 해석 지침, 확인 요청 답변, 질의 현황, 변경 이력, 권한, 체계 점검
js/manage.js        관리자 페이지(나의 업무): 규정별 정/부 담당자 지정, 신설·개정 파일 업로드/삭제(IndexedDB)
js/history.js       개정 이력: 조문 단위 버전 데이터(HISTORY) + 관리자 등록분, 신구대비·과거 시점 원문 복원·조문 옆 개정 표시
js/gemini.js        Gemini 연동: System Prompt(출처·가드레일·마크다운 포맷), 프록시/직접 호출, 마크다운 렌더
server/proxy.mjs    로컬 프록시(Node, 무의존성): API 키를 환경변수로만 보관하고 정적 파일도 서빙
js/annex.js         위임전결규정 별표 제1호 「위임전결기준표」: 양식만 실제 구조, 내용은 가상. 검색·결재라인 계산·근거 조문 이동
css/pages.css       위 화면들의 스타일
js/demo-answers.js  시연 모드 사전 작성 답변(DEMO_QA) + 정규화 질문→응답/규정키 매핑
js/i18n.js          다국어(한/영): 언어 선택, UI 사전(KO_EN), 정형 문구 패턴(PATTERNS), 렌더된 DOM 자동 번역(MutationObserver)
js/i18n-data.js     규정명·분류·팀·조문 제목·요약·추천 질문·공지 영문 사전(DATA_EN)
js/i18n-admin.js    관리자 페이지·시스템 관리·체크리스트·오류 메시지 영문 사전
js/i18n-body*.js    조문 본문·표 셀 영문 사전(BODY_EN). body=TES-105/111, 2=201/201-1A/203, 3=201-1B/216, 4=212/212-2/208
js/i18n-answers.js  시연 답변의 영문본(EN_A). 조문 번호·금액·일수는 한국어 답변과 동일해야 한다
js/app.js           앱 로직: 렌더링, 검색, 하이라이트, AI 공급자(live/demo), 담당자 화면
scripts/verify-quotes.js  인용 검증: node scripts/verify-quotes.js (답변 수정 후 반드시 실행)
assets/             대한전선 공식 CI (라이트/다크)
```

빌드 도구 없음. 정적 파일 그대로 GitHub Pages에 배포한다. 프레임워크를 도입하지 말 것
(도입이 필요하면 먼저 사람에게 확인).

## ⚠️ 현재 가장 큰 문제: GitHub에서 AI가 동작하지 않음

`js/app.js`의 AI 호출은 `window.claude.use("sample")` — Claude 아티팩트 런타임 전용이다.
GitHub Pages에서는 `window.claude`가 없어서 헤더에 "Offline / 열람 전용"이 뜨고 질문이 안 된다.
규정 목록·원문 열람·하이라이트(공지 클릭 시)는 정상 동작한다.

**API 키를 코드에 넣는 방식은 절대 금지.** 공개 저장소·공개 페이지라 그대로 노출된다.

## ✅ 시연 모드 (구현 완료 — 아래는 원래 명세)

`window.claude`가 없으면 헤더에 "Demo / 시연 모드"가 뜨고 `PROVIDERS.demo`가 답한다.
답변을 추가·수정하면 `node scripts/verify-quotes.js`로 quote 일치와 노출 질문 커버리지를 확인할 것.


API 키 없이 공개 주소에서 AI 흐름을 보여주기 위해 사전 작성 답변을 쓴다.

1. **응답 공급자 추상화.** `ask()`와 `routeAsk()`가 `SAMPLE.json()`을 직접 부르지 않게 하고,
   공급자를 선택하게 한다.
   - `window.claude`가 있으면 기존 방식 (아티팩트에서 열었을 때)
   - 없으면 시연 모드
2. **`js/demo-answers.js` 신설.** 질문 문자열(공백·문장부호 정규화) → 응답 객체.
   응답 스키마는 기존 LLM 출력과 동일하게 유지:
   `{answer, citations:[{id, quote}], related:[규정키], needsOwner, ownerQuestion}`
   라우팅용으로 `{질문 → 규정키}` 매핑도 둔다.
3. **다룰 질문.** `app.js`의 `CHIPS`(4) + `FAQ`(10) + 적재 규정별 `starters`(각 3개). 중복 제외 약 30개.
   최소 1개는 `needsOwner:true`로 만들어 확인 요청 흐름을 보여줄 것.
4. **등록되지 않은 자유 질문.** `retrieve()` 결과 상위 3개 조문을 인용으로 보여주고
   "시연 모드에서는 준비된 질문에만 AI가 답합니다. 관련 조문을 찾았습니다." 라고 답한다.
   통합 검색 라우팅은 모든 규정군에 `retrieve()`를 돌려 점수가 가장 높은 규정으로 보낸다.
5. **인용 검증 스크립트 `scripts/verify-quotes.js`.** node로 실행. demo-answers의 모든 quote가
   해당 조문 본문의 정확한 부분 문자열인지 검사하고, 아니면 실패 처리.
   (운영 설계의 "인용 검증 계층"을 빌드 단계에서 보여주는 장치이기도 하다.)
6. **헤더 상태 배지.** 시연 모드일 때 "Demo / 시연 모드"로 표시. 초록 펄스는 유지해도 됨.
7. 응답 전 600~900ms 지연을 두어 "근거 조문을 찾는 중" 표시가 보이게 한다.

## 다음 작업 2순위: 에이전트다운 기능

- **기안문 초안 생성.** 위임전결·출장 관련 답변 아래 "이대로 기안문 만들기" 버튼.
  제목, 목적, 전결 항목(TES-105 제7조 표의 구분), 전결권자, 근거 조문, 첨부 필요서류가
  채워진 전자결재 초안을 보여주고 복사할 수 있게. 원래 문제의식에 대한 직접적인 답이다.
- **확인 요청 루프 닫기.** 담당자 화면에서 확인 요청에 답을 입력 → 해당 규정의
  해석 지침(ownerPrompt)에 자동 추가 → 같은 질문 재질의 시 바로 답변.
- 근거 조문 포함 복사 (Teams로 팀장에게 전달하는 실무 흐름)
- 답변 하단 면책 문구: "규정 해석의 최종 권한은 주관부서에 있습니다"

## 개정 이력 (조문 단위 버전 관리)

- 데이터: `js/history.js`의 `HISTORY`(기본, 과거 조문은 가상) + 관리자 페이지 등록분(localStorage `rg:regVersions`).
  운영 시 `article_version` 테이블로 옮긴다.
- change의 `after`는 현행 조문과 정확히 일치해야 한다. `scripts/verify-quotes.js`가 검사한다.
- 규정 창구 원문 탭 옆 "개정 이력" 탭: 버전 타임라인, 신구대비(바뀐 부분만 강조), 과거 시점 원문(효력 없음 띠).
- 조문 제목 옆 "YYYY.MM.DD 개정" 표시 → 누르면 해당 버전 신구대비.
- 관리자 페이지에서 개정 이력 등록 시 "현행 조문에 반영"하면 조문 본문과 시행일이 바뀐다.
- 준비된 답변의 근거 문장이 현행 조문과 달라지면 답변 위에 "근거 조문이 개정되었습니다" 경고(실행 시 인용 검증).

## 실시간 AI (Gemini)

- 기본값은 시연 고정. 설정에서 "실시간 AI(Gemini)"를 켜면 `PROVIDERS.gemini`가 답한다.
- 호출 실패는 전부 catch해서 `PROVIDERS.demo`(검증된 사전 답변)로 전환하고 답변 위에 사유를 표시한다.
- API 키는 저장소·소스에 두지 않는다. 프록시(`server/proxy.mjs`, 환경변수 `GEMINI_API_KEY`)가 원칙이고,
  설정 화면의 키 입력은 로컬 테스트용(그 브라우저에만 저장). 배포 워크플로가 공개본에 빈 `js/env.js`를 넣는다.
- System Prompt: 대한전선 사내규정 안내 역할, 제공된 조문만 근거, 출처 조항 명시,
  규정 외 질문 거절 문구, `### 📌 관련 규정 / ### 📝 핵심 요약 / ### 💡 상세 내용 및 절차` 포맷.

## 구현된 기능

- 접이식 사이드바 (256px ↔ 64px, 0.3s, 접힘 시 툴팁). 메뉴 3그룹 6항목
- 헤더: taihan CI + 서비스명 + AI 연결 상태 + 알림/설정 + 프로필(경영기획팀 이순신)
- 홈: AI 검색 히어로(56px 입력창, 퀵 칩 4개) + 3칸 대시보드(카테고리/FAQ TOP5/개정 공지)
- 개정 공지 항목 클릭 → 해당 규정 열고 개정 문장 하이라이트
- 카테고리별 규정: 업무분류 8탭, 50건, 본규정 아래 하위지침 들여쓰기, 우측에 주관팀·시행일
- 규정 창구: 좌 AI 상담 / 우 원문. 근거 조문 버튼 → 자동 스크롤 + 문장 하이라이트
- 본규정+하위지침 탭, 근거가 지침에 있으면 탭 자동 전환
- 답변이 다른 규정을 필요로 하면 "○○에서 이어 확인 →" 버튼
- 담당자 화면: 해석 지침 편집(localStorage), 확인 요청 목록, 규정 체계 점검

## 규정 데이터 규칙

- 조문 본문은 **전부 가상**. 규정명·번호·주관부서·시행일은 실제 사내 게시판 기준(시행일=게시판 등록일).
- 실제 사규 원문을 이 저장소에 넣지 말 것. 공개 저장소다.
- 조문 ID 규칙: `{규정번호}-{조}` (예: `105-7`, `111-B1`=별표1). 하위지침은 `2011A-4`, `2122-8` 식.
- 표가 있는 조문은 body에 `"TABLE"` 자리표시자 + `table:{head,rows}`.
- 다른 규정과 엮이는 조문은 `xref`에 안내 문구.
- 적재 규정 10문서(8개 규정 + 지침 2개):
  TES-105 위임전결 / TES-111 직제 / TES-201 인사(+201-1 인사위원회 운영지침) / TES-203 복무 /
  TES-201-2 복리후생제도 세부지침 / TES-216 출장 및 여비 / TES-212 보안업무(+212-2 정보시스템 보안지침) /
  TES-208 교육훈련
- 내부 키 주의: 원래 TES-201-1 번호가 두 문서에 중복이라 키를 `TES-201-1A`(인사위원회), `TES-201-1B`(복리후생)로 구분했다. 시연 데이터에서는 복리후생 세부지침 번호를 TES-201-2로 바꿨지만 내부 키는 그대로 둔다.

### 시연용으로 맞춰둔 내용
- 직제규정은 개정 공지와 맞춤: 제2장 제6조②(안전총괄대표 신설), 제4장 제12조①(업무 범위), 별표1. 시행 2026.08.01.
- 복무규정 제14조⑤ 야근 식대: 1인 1회 15,000원, 익월 5영업일까지 정산 (임의 수치)

### 교차 연결 (시연 포인트)
- 경조사: 휴가일수(복무 제17조) ↔ 경조금(복리후생 세부지침 제4조)
- 휴직 중 복지: 휴직 종류(복무 제19조) ↔ 정지 범위(복리후생 제25조, 육아휴직 예외)
- 대결: 대행자(직제 제15조) ↔ 권한(위임전결 제9조)
- 재택: 신청(복무 제13조) ↔ 보안(정보시스템 보안지침 제12조)
- 생성형 AI: 이용 기준(지침 제8조) ↔ 정보등급(보안업무 제5조)
- 출장: 명령(출장여비 제5조) ↔ 전결권(위임전결 제7조)
- 경력직 승진: 경력 인정(인사 제10조) ↔ 소요연수(인사 제22조)

## 디자인

- 사이드바 #0f172a, 본문 배경 #f8fafc, 카드 흰색 + #e2e8f0 테두리, 모서리 12px
- 액센트 인디고 #4f46e5 (+ 블루 #2563eb). 하이라이트도 인디고 계열
- Noto Sans KR 단일 서체. 세리프 쓰지 않음
- 섹션 제목: 아이콘 박스 + 제목 + 회색 설명
- 색상은 `css/style.css` 상단 CSS 변수로만 바꿀 것

## 운영 전환 시 백엔드 (대회에선 설계서로만 제시)

```
[브라우저] → [FastAPI, 사내망]
              ├ 규정 라우팅
              ├ 하이브리드 검색 (BM25 + 임베딩)
              ├ 프롬프트 조립 (담당자 지침 → 조문 → 출력 스키마)
              ├ LLM 호출 (사내 승인 LLM: ChatGPT Enterprise 계열 API 또는 Azure OpenAI)
              ├ 인용 검증 ★ quote를 원문과 대조, 불일치 인용은 버리고 전부 실패 시 답변 차단
              └ 로깅
           → [PostgreSQL + pgvector]
```
데이터 모델: regulation / article / article_version / owner_guideline / query_log / escalation / feedback
(`article_version` 분리가 핵심 — 개정 이력 추적과 과거 답변 근거 변경 감지)
API 키 확보 여부 미확인. ChatGPT Enterprise 라이선스와 API는 별개 상품.

## 발견된 규정 체계 이슈 (제안서 소재)

- TES-201-1 번호 중복 → 시연 데이터에서는 복리후생 세부지침을 TES-201-2로 변경
- 예산관리규정이 8xx인데 제11조 업무분류에 8번 없음
- 취업규칙 번호 미부여 → 시연 데이터에서는 TES-201-3(인사규정_취업규칙)로 부여
- 보안 규정군 외 대부분 2016~2017 등록 — 게시판만으로는 최신 개정 여부를 알 수 없음

## 작업 방식

- 변경 후 브라우저에서 `index.html`을 직접 열어 확인 (로컬 서버 불필요, 필요하면 `npx serve .`)
- 기능 추가 전에 설계 원칙 3개 중 어디에 해당하는지 생각할 것. 어디에도 안 걸리면 넣지 않는다.
- 커밋은 기능 단위로 작게.
